# XDB Codecs

Low-level serialization for key-value storage backends.

```
codec/
├── codec.go       # Core interfaces (KVEncoder, KVDecoder)
├── json/          # JSON codec
└── msgpack/       # MessagePack codec
```
