syntax = "proto3";

package acme.user;

// email (number 2) is renamed to contact_email, keeping its proto number — a
// proto-detected rename.
message User {
  string name = 1;
  string contact_email = 2;
}
