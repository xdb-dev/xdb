# XDB API

HTTP API layer for XDB operations.

```
api/
├── tuple_api.go     # Tuple CRUD endpoints
├── record_api.go    # Record CRUD endpoints (stub)
├── schema_api.go    # Schema management
└── types.go         # Request/response types
```

**Note:** RecordAPI is a stub implementation pending future iteration.
