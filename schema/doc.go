// Package schema defines schema definition types for XDB.
//
// A schema describes the expected structure of records within
// a namespace. Schemas can operate in open mode (advisory, any
// fields allowed) or strict mode (only declared fields accepted).
package schema
