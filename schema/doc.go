// Package schema defines schema definition types and validation for XDB.
//
// A schema describes the expected structure of records within
// a namespace. Schemas can operate in flexible mode (advisory, any
// fields allowed), strict mode (only declared fields accepted),
// or dynamic mode (auto-infers new fields from data).
package schema
